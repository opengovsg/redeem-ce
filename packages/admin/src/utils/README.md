# Usage

Utils should only be used for string and array operations, separate from helper functions (although there may be some overlap). Think of utils as functions for related types of "objects," such as strings and arrays. Some examples include: capitalizing the first letter of a string, counting the number of unique items in an array, and performing special string operations like delimiting a string into commas.
